# URL of your deployed backend (see /backend). During local development
# it defaults to http://localhost:4000, so this is only needed when you
# build for production.
VITE_API_URL=https://your-backend-name.onrender.com
